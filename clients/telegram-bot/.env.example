# Add this to your root .env (or a .env in this directory — both are loaded by core.config)
TELEGRAM_BOT_TOKEN=your_token_here

# Backend port — only needed if you changed the default (5454)
# BACKEND_PORT=5454
