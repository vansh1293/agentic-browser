# Telegram Bot

Reserved for the future Python Telegram bot client.

It will live under `clients/` because it is another user-facing surface for the root Python backend.

## To Do 
- [ ] each message new thread
- [ ] if reply then only then continue the thread
